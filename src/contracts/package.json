{
  "name": "libr-contracts",
  "version": "0.1.0",
  "description": "Smart contracts for the LIBR protocol",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "node": "hardhat node",
    "clean": "hardhat clean",
    "coverage": "hardhat coverage",
    "lint": "solhint 'contracts/**/*.sol'",
    "lint:fix": "solhint 'contracts/**/*.sol' --fix"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "smart-contracts",
    "solidity",
    "libr",
    "social-network",
    "censorship-resistant"
  ],
  "author": "DALDA IITJ",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.1",
    "@openzeppelin/test-helpers": "^0.5.16",
    "hardhat": "^2.19.2",
    "solhint": "^5.1.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
